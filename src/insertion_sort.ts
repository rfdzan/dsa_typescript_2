export {insertionSort};
function insertionSort(arr: number[]) {
  for (let index = 1; index < arr.length; index++) {
    const tempValue = arr[index];
    const position = index - 1;
  }
}
insertionSort([5, 6, 7, 2, 3]);
